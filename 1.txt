docker run -d -p 9000:9000 -p 9443:9443 --name portainer --restart always portainer/portainer-ce


curl -fsSL https://raw.githubusercontent.com/sharp-and-tannan/steup/refs/heads/main/docker.sh | bash


docker run --name postgres-container -e POSTGRES_USER=root -e POSTGRES_PASSWORD=toor -p 5432:5432 -d postgres

docker run -d --name shreno-dms-backend -p 3001:3001 -e CORS_ORIGIN=http://172.22.60.32:5173 -e FRONTEND_URL=http://172.22.60.32:5173 -e BACKEND_URL=http://172.22.60.32:3001 -e DATABASE_URL="postgresql://root:toor@host.docker.internal:5432/dms" -v /d/dms:/app/data sharpandtannan/shreno-dms-backend:1

docker run -p 5173:80 sharpandtannan/shreno-dms-frontend:1

VITE_BACKEND_URL=http://172.22.60.32:3001




networkingMode=Mirrored[wsl2]

[network]
generateResolvConf = false
address = 192.168.100.1
[wsl2]
networkingMode=Mirrored
[experimental]
hostAddressLoopback=true


wsl --install -d kali-linux

https://drive.google.com/file/d/1V9if5Xs4SQ7YG_Xf8hi2eh9ZV9WyD4Yf/view?usp=drive_link






import express from "express";
import path from "path";
import { fileURLToPath } from "url";

const app = express();
const PORT = process.env.PORT || 5173;

// Fix __dirname in ES module
const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const distPath = path.join(__dirname, "dist");

app.use(express.static(distPath));

app.get("*", (req, res) => {
  res.sendFile(path.join(distPath, "index.html"));
});

app.listen(PORT, () => {
  console.log(`Vite frontend running on http://localhost:${PORT}`);
});
